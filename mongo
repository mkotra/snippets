################################################################################################################
db.orders.aggregate( [ 
        { $project: { dateDifference: { $subtract: [ "$modifiedTimestamp", "$creationTimestamp" ] } } } ,
        { $match: { dateDifference : {$gte: 3000} }}
] )
################################################################################################################
